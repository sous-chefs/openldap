---
driver:
  name: vagrant

provisioner:
  name: chef_infra
  product_name: chef
  product_version: latest
  channel: stable
  chef_license: accept
  enforce_idempotency: true
  multiple_converge: 2
  deprecations_as_errors: true

verifier:
  name: inspec
  sudo: true

platforms:
  - name: almalinux-8
  - name: amazonlinux-2023
  - name: debian-12
  - name: fedora-latest
  - name: opensuse-leap-15
  - name: oraclelinux-8
  - name: rockylinux-8
  - name: ubuntu-22.04
  - name: ubuntu-24.04

x-verifiers:
  default: &default_verifier
    inspec_tests:
      - path: test/integration/default
  tls_enabled: &tls_enabled_verifier
    inspec_tests:
      - path: test/integration/tls_enabled
  type_provider: &type_provider_verifier
    inspec_tests:
      - path: test/integration/type_provider
  type_consumer: &type_consumer_verifier
    inspec_tests:
      - path: test/integration/type_consumer

suites:
  - name: default
    verifier: *default_verifier
  - name: tls-enabled
    named_run_list: ssl
    verifier: *tls_enabled_verifier
  - name: type-provider
    named_run_list: provider
    verifier: *type_provider_verifier
  - name: type-consumer
    named_run_list: consumer
    verifier: *type_consumer_verifier
